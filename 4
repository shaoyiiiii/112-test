<?php
// 資料庫連線設定
$server = "localhost";
$user = "root";
$password = ""; // 預設密碼為空，依實際情況修改
$database = "mydb";

// 建立連線
$conn = new mysqli($server, $user, $password, $database);

// 檢查連線是否成功
if ($conn->connect_error) {
    die("連線失敗：" . $conn->connect_error);
}
?>
